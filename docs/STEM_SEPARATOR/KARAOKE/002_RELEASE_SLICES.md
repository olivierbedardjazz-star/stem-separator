# 0.1.2 / 102 completion plan

Authorized September 15, 2026: execute locally through signed installation, exact-source GitHub proof, Apple notarization, publication and real Sparkle update. Existing 0.1.0/0.1.1 release bytes remain immutable. No new private keys, paid services or repository secrets.

## Slice 1 — Durable cleanup
Outcome: no intentional audio cache; recover owned partial jobs even after temporary metadata is purged. Files: JobWorkspace.swift, SeparationSession.swift, SeparationStore.swift, StemOutputPanelView.swift, AppEnvironment.swift, Tests/SeparationTests.swift, Tests/Performance/{NativeProbe.swift,build_probe.py,run_suite.py,capacity_tests.py}. Persist only small ownership/bookmark/lock records under Application Support/CleanupRecovery; audio stays in temporary jobs and owned destination staging. A journal lock protects active jobs. Validate UUID, ownership, symlinks and bookmark before deletion; retain records on unavailable disks or failed deletion. Delete working payload before markers. Surface pending cleanup and retry on launch/mount/new job. Check each volume before preparation. Test crash, record survival without temp workspace, denied permissions/retry, mismatched owners, symlinks, cancelled/failed/success jobs and disk capacity. No licence change. Stop on unowned deletion, lost retry records or hidden normal-path leftovers.

## Slice 2 — Long-duration qualification
Outcome: measured 10- and 20-minute native end-to-end runs in both modes, plus cancellation during inference and writing. Files: performance harness above and new release qualification script/report. Fixed synthetic fixtures test duration/memory/storage deterministically; separately identify the absence of a real vocal music listening corpus. Sample process-tree RSS, timing, temporary/destination bytes and swap. Validate frame counts, PCM24, channel/rate, finite output and original hash. Delete generated audio after verification, retain metrics only. Stop at 12 GiB process RSS, 1 GiB system swap growth, low disk space, timeout or invalid output. No dependency changes. Synthetic success does not establish subjective musical quality or other hardware performance.

## Slice 3 — Local signed installation
Outcome: exact reviewed 0.1.2/102 source, bundled MPS/karaoke app, nested Developer ID signatures and provisional signed DMG installed to a new test-owned path. Files: release_pipeline.py, ci_clean_proof.sh, verify_published_installer.sh, UITests/StemSeparatorUITests.swift, release docs. Add karaoke to all installed-runtime checks and both UI modes. Source allowlist audit, local commit, archive, nested signing, manifest regeneration, signature/notices/legal checks, offline MPS tests, mounted-DMG copy and installed UI workflow. Existing archives untouched. Stop on signature/runtime/UI failure or keychain access denial; never claim provisional Gatekeeper acceptance before notarization.

## Slice 4 — Clean GitHub proof
Outcome: successful exact-source builds on macOS 15 and 26. Files: .github/workflows/clean-build.yml, ci_clean_proof.sh, source tests. Push only reviewed source, record SHA/run IDs and download diagnostics. GPU-less runners explicitly skip inference; local physical MPS proof remains mandatory. No Apple/Sparkle secrets leave this Mac. Stop on source drift, dependency/provenance failure or either CI failure; fix forward before freezing a new candidate.

## Slice 5 — Apple and immutable assets
Outcome: accepted app and DMG, stapled tickets, Gatekeeper pass, signed Sparkle ZIP/appcast with minimum macOS15.1, corresponding sources/checksums. Existing scripts and release-evidence/0.1.2-102 record exact fingerprints/submission IDs. Submit only after local signed UI and clean CI gates. Poll existing submissions; no duplicate submission on delay. Stop on rejection, changed bytes or invalid signatures. No old release replacement; macOS14 users remain on compatible release.

## Slice 6 — Publication and updater
Outcome: new public release, anonymous asset hash verification, downloaded/quarantined installer proof on both hosted OS versions, actual 0.1.1→0.1.2 Sparkle install/relaunch and post-update stem/karaoke workflow. Use a fresh copy of the predecessor in a new test directory; preserve predecessor archive, preferences and user data. Files: release pipeline/status scripts, UI tests, sanitized acceptance report. Stop on missing update, OS minimum mismatch, changed bytes, installation/signature/test failure. Do not mark complete until evidence exists. Retain final candidate and metrics; no modification of prior handoff artifacts.
