# Local product proof

See the consolidated [acceptance record](release-acceptance.md) for native Debug/Release tests, each signed installed UI gate before GitHub, offline inference/control proofs, the three-minute measurement, and the later local Debug-loader rerun limitation. Full logs and artifact-bound records remain under local `release-evidence`. No unsigned/ad-hoc test build is represented as Developer ID distribution proof.
